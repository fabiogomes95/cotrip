<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <!-- Mesma bolinha da marca no topo do app: sol no canto superior esquerdo
       derretendo no verde-azulado. Ver `.brand .mark` em globals.css. -->
  <defs>
    <radialGradient id="g" cx="34%" cy="30%" r="78%">
      <stop offset="0%" stop-color="#e1823b" />
      <stop offset="78%" stop-color="#0f857a" />
    </radialGradient>
  </defs>
  <rect width="32" height="32" rx="7" fill="#f7f3eb" />
  <circle cx="16" cy="16" r="10.5" fill="url(#g)" />
</svg>
